# Engine

The engine layer contains the mechanics that drive transformation inside NE3ULA.

Core areas:

- `E3/` is the canonical transformation engine.
- `execution/` is a lightweight incubation space for cross-cutting execution
  concepts that are not yet stable enough to canonize inside a fixed E3
  subsystem.
